// Code generated by goa v3.13.2, DO NOT EDIT.
//
// Module service
//
// Command:
// $ goa gen goa.design/model/mdl/service/design -o mdl/service

package module

import (
	"context"
	"io"

	goa "goa.design/goa/v3/pkg"
)

// Service is the Module service interface.
type Service interface {
	// List the model modules in the current Go workspace
	ListModules(context.Context) (res []*Module, err error)
	// WebSocket endpoint for subscribing to updates to the model
	Subscribe(context.Context, *Module, SubscribeServerStream) (err error)
	// Stream the model JSON, see https://pkg.go.dev/goa.design/model/mdl#Model
	GetModel(context.Context, *Module) (body io.ReadCloser, err error)
	// Stream the model DSL, save it, compile it and return the corresponding JSON
	Compile(context.Context, *Module, io.ReadCloser) (res Model, err error)
	// Stream the model layout JSON saved in the SVG
	GetLayout(context.Context, *Module) (body io.ReadCloser, err error)
	// Save the SVG streamed in the request body
	WriteDiagram(context.Context, *Module, io.ReadCloser) (err error)
}

// ServiceName is the name of the service as defined in the design. This is the
// same value that is set in the endpoint request contexts under the ServiceKey
// key.
const ServiceName = "Module"

// MethodNames lists the service method names as defined in the design. These
// are the same values that are set in the endpoint request contexts under the
// MethodKey key.
var MethodNames = [6]string{"ListModules", "Subscribe", "GetModel", "Compile", "GetLayout", "WriteDiagram"}

// SubscribeServerStream is the interface a "Subscribe" endpoint server stream
// must satisfy.
type SubscribeServerStream interface {
	// Send streams instances of "Model".
	Send(Model) error
	// Close closes the stream.
	Close() error
}

// SubscribeClientStream is the interface a "Subscribe" endpoint client stream
// must satisfy.
type SubscribeClientStream interface {
	// Recv reads instances of "Model" from the stream.
	Recv() (Model, error)
}

// Model is the result type of the Module service Subscribe method.
type Model string

// Module is the payload type of the Module service Subscribe method.
type Module struct {
	// Design Go module
	GoMod string
}

// MakeCompilationFailed builds a goa.ServiceError from an error.
func MakeCompilationFailed(err error) *goa.ServiceError {
	return goa.NewServiceError(err, "compilation_failed", false, false, false)
}
